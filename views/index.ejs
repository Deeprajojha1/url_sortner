<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Url shortner project</title>
    <style>
    body {
        font-family: Arial, sans-serif;
        background: #2d8bbd;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    .container {
        width: 400px;
        padding: 40px;
        background: rgb(62, 51, 51);
        border-radius: 10px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        animation: slideIn 1s ease forwards;
    }
    /* .container:hover {
        background-color: #2a343e;
        transform: scale(1.05);
        color: white;
    } */

    @keyframes slideIn {
        0% {
            transform: translateY(-30px);
            opacity: 0;
        }
        100% {
            transform: translateY(0);
            opacity: 1;
        }
    }

    h1{
        text-align: center;
        color: rgb(136, 205, 80);
        margin-bottom: 20px;
    }
    form {
        display: flex;
        flex-direction: column;
        gap: 15px;
    }

    label {
        font-weight: bold;
        color:aliceblue
    }

    input[type="text"] {
        padding: 10px;
        border-radius: 5px;
        border: 1px solid #ccc;
        font-size: 16px;
    }

    button {
        padding: 10px;
        border: none;
        border-radius: 5px;
        background-color: #007bff;
        color: white;
        font-size: 16px;
        cursor: pointer;
        transition: background-color 0.3s, transform 0.3s;
    }

    button:hover {
        background-color: #0056b3;
        transform: scale(1.05);
    }

    p {
        margin-top: 20px;
        font-size: 16px;
        color:#ccc
    }

    a {
        color: #007bff;
        text-decoration: none;
    }

    a:hover {
        text-decoration: underline;
    }
</style>

</head>
<body>
    <div class="container">
        <h1>Welcome to the URL Shortener Project</h1>
        <form action="/shorten" method="POST">
            <label for="labelid">Enter long url</label>
            <input type="text" id="labelid" name="longUrl" required>
            <button type="submit">Shorten URL</button>
        </form>
        <% if (shortenedUrl) { %>
            <p>Short Url :- <a href="<%= shortenedUrl %>" target="_blank"><%= shortenedUrl %></a></p>

        <% } %>
    </div>
</body>
</html>